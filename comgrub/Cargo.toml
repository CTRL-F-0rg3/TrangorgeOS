[package]
name = "comgrub"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel" }

[build-dependencies]
nasm-rs = "0.3"

[profile.dev]
panic = "abort"

[dependencies]
[profile.release]
panic = "abort"
lto = true