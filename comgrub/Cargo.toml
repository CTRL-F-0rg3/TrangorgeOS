[package]
name = "comgrub"
version = "0.1.0"
edition = "2021"

[dependencies]
# kernel jako biblioteka statyczna — comgrub będzie go ładować
kernel = { path = "../kernel" }

[build-dependencies]
nasm-rs = "0.3"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true