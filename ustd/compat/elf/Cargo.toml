[package]
name = "tgs-elf"
description = "ELF64 parsing for the TrangorgeOS Linux compatibility layer."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
name = "tgs_elf"
crate-type = ["rlib"]
doctest = false

[dependencies]
