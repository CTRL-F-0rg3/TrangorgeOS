[package]
name = "comlimine"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel = { path = "../kernel" }

[build-dependencies]
cc = "1.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true