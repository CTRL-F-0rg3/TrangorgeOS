# TrangorgeOS/Cargo.toml
[workspace]
resolver = "2"
members = [
    "kernel",
    "comgrub",
    "comlimine",
    "trangorgelibc",
]
[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
