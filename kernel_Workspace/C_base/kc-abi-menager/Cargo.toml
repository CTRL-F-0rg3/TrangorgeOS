[package]
name = "kc-abi-menager"
version = "0.1.0"
edition = "2021"

[lib]
name = "kc_abi_menager"
path = "src/lib.rs"

[dependencies]
# Wyższa warstwa zależy od jądra / core:
kernel = { path = "../../kernel" }
kstd_core = { path = "../../core/kstd_core" }