[workspace]
resolver = "2"
members = [
    "base/*",
    "C_base/*",
    "core/*",
    "kernel",
    "Odin_base/*", "kernel-bin",
]

[profile.release]
panic = "abort"
opt-level = 2

[profile.dev]
panic = "abort"
