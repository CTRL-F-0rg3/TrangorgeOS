[package]
name = "tr-init"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "init"
path = "src/main.rs"
# Bare-metal (`x86_64-unknown-none`) nie ma `std` ani crate'a `test`.
test = false

[dependencies]
trangorgelib = { path = "../../trangorgelibc" }