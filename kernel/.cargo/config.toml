[unstable]
json-target-spec = true
build-std-features = ["compiler-build-mem]
build-std = ["core", "compiler_builtins"]

[build]
target = "x86_64-unknow-none.json"

[target.'cfg(target_os = "none")']
runner = "bootimage runnet" 